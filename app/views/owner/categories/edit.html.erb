<h1>Edit Category</h1>

<%= render 'form', category: @category, url: owner_category_path(@category) %>

<%= link_to 'Show', owner_category_path(@category) %> |
<%= link_to 'Back', owner_categories_path %>
