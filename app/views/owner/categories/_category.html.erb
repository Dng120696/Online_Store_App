<tr>
  <td><%= category.title %></td>
  <td><%= category.admin.email %></td>
  <td><%= link_to 'Show', owner_category_path(category) %></td>
  <td><%= link_to 'Edit', edit_owner_category_path(category) %></td>
  <td><%= link_to 'Destroy', owner_category_path(category),
      data: {turbo_method: :delete,turbo_confirm: 'Are you sure you want to delete this user?'} %></td>
</tr>
