Home Page hello there
<div class="flex items-center gap-4">
  <% if !user_signed_in? && !admin_signed_in? %>
    <%= link_to 'User Sign In', new_user_session_path, class: ' ' %>
    <%= link_to 'Admin Sign In', new_admin_session_path, class: ' ' %>
  <% else %>
    <% if current_user %>
      <%= link_to "Logout", destroy_user_session_path, data: {
                turbo_method: :delete,
                turbo_confirm: "Are you sure?"
              } %>
    <% else %>
      <%= link_to "Logout", destroy_admin_session_path, data: {
                turbo_method: :delete,
                turbo_confirm: "Are you sure?"
              } %>
    <% end %>
  <% end %>
</div>